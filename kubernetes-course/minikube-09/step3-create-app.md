Lo que haremos ahora, es generar una imagen de una simple aplicacion en python.

`git clone https://github.com/mguazzardo/apiflask`{{execute}}

y nos dirigimos a ese directorio

`cd apiflask`{{execute}}
